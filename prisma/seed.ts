/**
 * Optional sample data — OFF by default.
 * Prefer an empty database so the UI only shows citizen-created content.
 * Run `npm run db:seed` only when you explicitly want demo fixtures.
 */
console.log(
  "Seed skipped: Janark starts empty. Create posts via the app, or edit prisma/seed.ts if you need fixtures.",
);
process.exit(0);
